git:
  depth: 1


language: cpp

notifications:
 email:
  on_success: never
  on_failure: never



env:

before_install:
- export cwd=$(pwd)
- export SCRIPT_DIRECTORY=$cwd/travis

- ls
#- travis_wait 45 
- travis_wait 45 /bin/bash $SCRIPT_DIRECTORY/geant4_build.sh 10.03.p03
# > $SCRIPT_DIRECTORY/geant4_install_log.txt 2>&1
- ls $HOME/geant4.10.03.p03/*
#- less $SCRIPT_DIRECTORY/geant4_install_log.txt

install:

cache:
- timeout: 2700
- directories:
  - $HOME/geant4.10.03.p03

script:

#/usr/bin/ctest -S $SCRIPT_DIRECTORY/linux15_lin64/linux-gcc_4.8.3_ITKOFF-geant4_10.03.p03-opengate-creatis-develop.cmake -VV --output-on-failure


- echo $cwd
- echo $SCRIPT_DIRECTORY
- ls
- echo $HOME

